# Repository Instructions

## Writing Format

- Write all project content in Markdown (`.md`) files.
- Prefer plain Markdown syntax over proprietary document formats.
- Do not create or commit exported binary documents as source material.
- If an export is needed for sharing, generate it outside version control.

